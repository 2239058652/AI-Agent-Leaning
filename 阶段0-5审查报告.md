# 阶段0-5 完成度审查报告

> 审查日期：2026-07-17
> 审查依据：`agent-dev-learning-plan-v3.md`（事件推进版 v3）
> 审查方式：逐阶段对照计划任务项与实际代码

---

## 总览

6个阶段均声称已完成（见 `ai-assistant/README.md` 和 `ai-assistant/CLAUDE.md`），但实际审查发现每个阶段都有未完成的任务项。核心问题是阶段3"敏感写工具"缺失，导致阶段3.5、阶段4.3 形成连锁缺失。

| 阶段 | 完成度 | 关键遗漏 |
|---|---|---|
| 阶段0 | 未做 | 没有 CRUD 主线项目、没有业务定义 |
| 阶段1 | 基本完成 | 未授权/无权限异常缺失；2个测试可能编译不过 |
| 阶段2 | 部分完成 | 错误码未区分、上下文截断缺失、成本估算缺失、结构化JSON缺失 |
| 阶段3 | 部分完成 | 敏感写工具缺失，安全边界机制只是空壳 |
| 阶段4 | 部分完成 | 敏感操作确认弹窗缺失 |
| 阶段5 | 部分完成 | Resource 未接入、Prompts 缺失、session隔离缺失 |

---

## 阶段0：确定主线项目和学习边界

**计划要求（v3 第21-49行）**：
- 0.1 选择一个已有 CRUD 项目作为改造对象
- 0.2 定义 Agent 要解决的业务问题（至少包含只读查询、聚合分析、敏感写操作）
- 0.3 定义贯穿全程的最小业务闭环
- 产物：业务场景说明、3个 Agent 任务样例、不做范围列表

**实际状态：未完成**

- 项目是从零搭建的通用 AI 助手，没有业务实体，没有 CRUD
- 工具是学习性质的 demo（查天气、查日期、计算器、查IP），不是业务工具
- 没有找到任何阶段0的产物文档

**影响**：后续阶段的"业务工具""敏感写操作"都因为阶段0没有定义而无法落地。

---

## 阶段1：Spring Boot 工程化补强

**计划要求（v3 第52-88行）**：
- 1.1 审查项目分层
- 1.2 补全统一异常处理（全局异常处理器、业务异常、参数校验异常、**未授权/无权限异常**）
- 1.3 梳理事务边界
- 1.4 补最小测试（1个 Service 正常场景、1个业务异常场景、1个 Controller 校验场景）

### 完成情况

| 任务 | 状态 | 说明 |
|---|---|---|
| 1.1 分层审查 | 完成 | Controller/Service/DTO/Tool/Config/Exception 分层清晰，Controller 无业务逻辑 |
| 1.2 全局异常处理 | 部分完成 | 有 `GlobalExceptionHandler`（`config/GlobalExceptionHandler.java`）+ `BusinessException`（`exception/BusinessException.java`）+ 参数校验异常处理。**未授权/无权限异常缺失**——计划在1.2中明确列出，实际没有对应异常类和处理器 |
| 1.3 事务边界 | 合理跳过 | 无数据库层，CLAUDE.md 已说明暂不需要 |
| 1.4 最小测试 | 部分完成 | 有4个测试文件，覆盖了正常/异常/校验场景。**但2个测试可能编译不过**（详见下方"代码问题"） |

### 测试清单

| 文件 | 覆盖场景 | 问题 |
|---|---|---|
| `ToolServiceTest.java` | 日期工具正常、计算器正常、除零异常、未知工具异常 | 构造 `ToolService` 传了3个参数，实际需要4个（缺少 `McpClientService`） |
| `ToolRegistryTest.java` | 白名单、工具定义、JSON生成、敏感标记 | 构造 `ToolRegistry` 传了1个参数，实际需要2个（缺少 `McpClientService`）；且断言 `get_weather_by_city` 在白名单中，但该工具未在 Registry 注册 |
| `ChatControllerValidationTest.java` | message 为空、为null、超长 | 无明显问题 |
| `ToolValidatorTest.java` | 必填参数缺失、类型正确、类型错误、非法JSON、可选参数缺失 | 无明显问题 |

---

## 阶段2：LLM API 底层调用

**计划要求（v3 第92-135行）**：
- 2.1 用 Java 原生 HttpClient 调用 LLM API（非流式）
- 2.2 实现流式 SSE 解析
- 2.3 增加基础错误处理（**超时、401/403、429、5xx**）
- 2.4 理解 Token 和上下文窗口（输入Token、输出Token、**上下文截断**、**成本估算**）
- 2.5 输出结构化 JSON（让模型返回固定 JSON 结构、处理 JSON 不合法情况）
- 产物：LlmClient、非流式demo、流式demo、错误处理策略、Token使用记录结构

### 完成情况

| 任务 | 状态 | 说明 |
|---|---|---|
| 2.1 HttpClient 非流式 | 完成 | `ChatService.chat()`（`service/ChatService.java:50`） |
| 2.2 SSE 流式解析 | 完成 | `ChatService.chatStreamInternal()`（`service/ChatService.java:93`），逐行读取、处理 [DONE]、推送给前端 |
| 2.3 基础错误处理 | 部分完成 | 只有 `statusCode != 200` 的通用处理（`ChatService.java:64` 和 `:144`）。**没有区分 401/403（鉴权）、429（限流）、5xx（服务端错误）**，没有超时重试策略 |
| 2.4 Token 和上下文 | 部分完成 | `ChatResponse` 有 `promptTokens`/`completionTokens` 字段，`parseResponse()` 提取了 token 数。**上下文截断逻辑缺失**（history 无限增长无处理），**成本估算缺失**（无价格配置、无成本计算） |
| 2.5 结构化 JSON 输出 | 未完成 | 完全没有实现。没有让模型返回固定 JSON 结构的代码，没有处理 JSON 不合法的逻辑 |

### 缺失产物
- 错误处理策略文档（区分 401/403/429/5xx）
- Token 使用记录结构（当前只是返回给前端，没有持久化）
- 结构化 JSON 输出功能

---

## 阶段3：Tool Use / Function Calling

**计划要求（v3 第138-182行）**：
- 3.1 理解 Tool Schema（名称、描述、JSON Schema 参数、必填字段）
- 3.2 实现本地工具注册（**至少2个只读工具、至少1个敏感写工具**）
- 3.3 实现模型触发工具调用（解析 tool call、执行本地方法、发回 tool result）
- 3.4 实现 Agent Loop（连续多工具、最大循环次数、超时、失败退出）
- 3.5 增加工具安全边界（白名单、参数校验、**敏感操作进入待确认状态**、不允许模型直接执行删除/修改）
- 产物：工具注册表、至少3个业务工具、Agent Loop、敏感操作待确认机制

### 完成情况

| 任务 | 状态 | 说明 |
|---|---|---|
| 3.1 Tool Schema | 完成 | `ToolDefinition`（`tool/ToolDefinition.java`）有 name/description/parameters/sensitive 字段 |
| 3.2 本地工具注册 | 部分完成 | 有3个只读工具：`get_current_date`、`calculate`、`get_ip`（`tool/ToolRegistry.java:39-78`）。**没有1个敏感写工具**——计划明确要求"至少1个敏感写工具"，实际所有工具 `sensitive=false` |
| 3.3 模型触发工具调用 | 完成 | `ChatService` 解析 `tool_calls` → `ToolService.execute()` → 结果发回模型（`service/ChatService.java:198-242`） |
| 3.4 Agent Loop | 完成 | 最大10轮循环（`ChatService.java:113`）、SseEmitter 超时300秒、失败发送 error 事件 |
| 3.5 工具安全边界 | 部分完成 | 白名单（`ToolRegistry.isAllowed()`）完成、参数校验（`ToolValidator`）完成。**敏感操作待确认只是预留机制**——`ToolDefinition.sensitive` 字段存在，`ToolService` 有拒绝敏感操作的代码（`tool/ToolService.java:67-69`），但因为没有敏感工具，这段代码从未被实际执行验证 |

### 缺失产物
- 至少1个敏感写工具
- 敏感操作待确认机制的完整链路（后端标记 → 前端确认 → 后端执行）

### 连锁影响
阶段3的"敏感写工具"缺失直接导致：
- 阶段3.5 敏感操作待确认无法实际验证
- 阶段4.3 敏感操作确认弹窗无法实现

---

## 阶段4：前端接入第一个 Agent 闭环

**计划要求（v3 第185-222行）**：
- 4.1 React 接入流式输出（SSE/Fetch Stream，支持加载中/输出中/完成/失败状态）
- 4.2 展示 Agent 执行链路（用户问题、模型思考、工具调用、工具结果、最终答案）
- 4.3 实现敏感操作确认（后端返回待确认、前端弹窗、用户确认后执行）
- 4.4 增加中断和重试

### 完成情况

| 任务 | 状态 | 说明 |
|---|---|---|
| 4.1 流式输出 | 完成 | `frontend/src/App.tsx` 用 fetch + ReadableStream，有 loading 状态切换 |
| 4.2 执行链路 Timeline | 完成 | 解析 `tool_call` 和 `tool_result` SSE 事件，渲染 Timeline（`App.tsx:336-356`） |
| 4.3 敏感操作确认 | 未完成 | 前端没有确认弹窗组件，后端没有待确认状态返回。CLAUDE.md 已承认"待加入敏感写操作后补做" |
| 4.4 中断和重试 | 完成 | `AbortController` 中断（`App.tsx:252`）+ `handleRetry` 基于最后一条用户消息重试（`App.tsx:260`） |

### 缺失产物
- 敏感操作确认弹窗（依赖阶段3的敏感写工具）

---

## 阶段5：MCP 协议基础

**计划要求（v3 第225-265行）**：
- 5.1 理解 MCP 三大原语（Tools、Resources、Prompts）
- 5.2 搭建最小 MCP Server (stdio)（暴露1个只读工具、**暴露1个 Resource**、用本地客户端调通）
- 5.3 接入 Claude Desktop 或其他 MCP 客户端（验证工具发现、工具调用、观察协议消息）
- 5.4 理解 stdio 和 Streamable HTTP
- 5.5 改造成 Streamable HTTP（多客户端连接、session 隔离、连接断开重连）
- 产物：stdio MCP Server、Streamable HTTP MCP Server、至少1个 Tool、**至少1个 Resource**、**session 隔离说明**

### 完成情况

| 任务 | 状态 | 说明 |
|---|---|---|
| 5.1 三大原语 | 部分完成 | Tools 完成。Resources 有 `ConfigResource` 类（`mcp-server/.../ConfigResource.java`）但**未接入 MCP Server**。Prompts **完全缺失** |
| 5.2 最小 MCP Server (stdio) | 部分完成 | `McpServerApp.java` 用 `StdioServerTransportProvider`，注册了 `get_weather` 工具。**Resource 未接入**（capabilities 只设了 `.tools(true)`，没有 resources）。stdio 客户端调通无验证记录 |
| 5.3 接入外部 MCP 客户端 | 未知 | 没有找到 Claude Desktop 配置文件或验证文档 |
| 5.4 stdio vs HTTP 理解 | 完成 | 代码两种传输方式都有，注释有说明 |
| 5.5 Streamable HTTP | 部分完成 | `McpConfig.java` 用 `HttpServletStreamableServerTransportProvider`，HTTP Server 可运行。**session 隔离无代码无文档**。**连接断开重连无处理**（只有 `keepAliveInterval(30s)`） |

### MCP Server 代码细节

| 文件 | 作用 | 问题 |
|---|---|---|
| `McpServerApplication.java` | Spring Boot 启动类（HTTP 版） | 注释写端口 8090，实际 `application.yaml` 配置 3190 |
| `McpServerApp.java` | stdio 版启动类 | 可运行，但未接入 Resource |
| `McpConfig.java` | HTTP 版 MCP Server 配置 | capabilities 只有 `.tools(true)`，未注册 Resource |
| `WeatherTool.java` | 天气查询工具实现 | 无问题 |
| `ConfigResource.java` | 配置资源读取 | **死代码**——有 `read()` 方法但从未被 McpConfig 注册到 MCP Server |

### 缺失产物
- 至少1个 Resource 接入 MCP Server
- Prompts 实现
- session 隔离说明
- 外部 MCP 客户端验证记录

---

## 代码问题汇总

### 1. 测试编译问题（高优先级）

`ToolServiceTest.java` 第28行：
```java
toolService = new ToolService(objectMapper, registry, validator);
```
`ToolService` 有 `@RequiredArgsConstructor`，final 字段包含 `ObjectMapper`、`ToolRegistry`、`ToolValidator`、`McpClientService` 四个。构造只传了3个参数，缺少 `McpClientService`，编译不过。

`ToolRegistryTest.java` 第19行：
```java
registry = new ToolRegistry(new ObjectMapper());
```
`ToolRegistry` 有 `@RequiredArgsConstructor`，final 字段包含 `ObjectMapper`、`McpClientService` 两个。构造只传了1个参数，缺少 `McpClientService`，编译不过。

`ToolRegistryTest.java` 第28行断言 `get_weather_by_city` 在白名单中，但 `ToolRegistry.init()` 只注册了 `get_current_date`、`calculate`、`get_ip` 三个本地工具，没有 `get_weather_by_city`。即使编译通过，此断言也会失败。

`ToolRegistryTest.java` 第54行断言 `assertEquals(4, toolsJson.size())`，但本地工具只有3个，MCP 工具在测试环境（无 McpClientService）不会注册，实际数量为3。

### 2. 死代码

`ToolService.java` 第102-165行的 `executeGetWeatherByCity()` 方法从未被调用。switch 语句（第86-91行）只处理 `get_current_date`、`calculate`、`get_ip`，没有 `get_weather_by_city`。天气查询通过 MCP 远程工具 `get_weather` 完成。

`mcp-server/.../ConfigResource.java` 全类从未被 McpConfig 注册到 MCP Server，`read()` 方法从未被调用。

### 3. 端口注释不一致

`McpServerApplication.java` 第11行注释写 `http://localhost:8090/mcp`，实际 `application.yaml` 配置端口为 3190。`ai-assistant` 的 `application.yaml` 连接地址也是 `http://localhost:3190/mcp`。

### 4. 前端工具标签不匹配

`frontend/src/App.tsx` 第55行 `TOOL_LABELS` 映射了 `get_weather_by_city`，但实际后端工具名是 `get_weather`（MCP 远程工具）。Timeline 中 MCP 工具调用会显示原始英文名而非中文标签。

---

## 连锁缺失分析

以下缺失项存在依赖关系，补一个可连带解决多个：

```
阶段0：没有定义敏感写操作业务
  ↓ 导致
阶段3.2：没有敏感写工具
  ↓ 导致
阶段3.5：敏感操作待确认机制无法实际验证
  ↓ 导致
阶段4.3：前端确认弹窗无法实现
```

```
阶段5.2：ConfigResource 写了但没接入 McpConfig
  ↓ 导致
阶段5.1：Resources 原语只有理论没有实践
```

---

## 建议补做顺序

1. **修测试编译问题**（阶段1）——给 ToolServiceTest 和 ToolRegistryTest 补 McpClientService mock，或改用 Spring 集成测试
2. **补1个敏感写工具**（阶段3）——连带激活 3.5 待确认机制
3. **补前端确认弹窗**（阶段4）——依赖第2步
4. **接入 Resource 到 McpConfig**（阶段5）——ConfigResource 代码已存在，只需注册
5. **补 Prompts**（阶段5）——最小实现1个提示模板
6. **补 session 隔离说明**（阶段5）——文档或代码注释
7. **补错误码区分**（阶段2）——401/403/429/5xx 分别处理
8. **补结构化 JSON 输出**（阶段2）——让模型返回固定格式
9. **清理死代码**——删除 `executeGetWeatherByCity`，修复端口注释和前端标签
