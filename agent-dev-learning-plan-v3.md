# Agent开发学习计划（前端转型 · 事件推进版 v3）

> 背景假设：前端开发者，自学过Java/Spring Boot但无真实后端项目经验，Agent开发零基础。
> 推进原则：不按周数推进。每个阶段只有在产物可运行、关键问题能解释、验收标准满足后，才进入下一阶段。
> 使用方法：每完成一项任务，把标题、代码、报错、设计疑问一起发给大模型 review。重点问“为什么这样设计”和“这种设计会在哪些场景失败”，不要只问“怎么写代码”。

---

## 总目标

最终做出一个可演示的 Agent 系统：

- 后端：Spring Boot
- 前端：React
- 模型调用：支持普通响应和流式响应
- Agent能力：支持多轮对话、工具调用、MCP工具接入、执行链路展示
- 工程能力：具备鉴权、敏感操作确认、可观测性、限流、Token成本统计、基础多租户隔离

---

## 阶段0：确定主线项目和学习边界

> 目的：先定一个贯穿全程的业务场景，避免每阶段都写一次孤立demo。

- [ ] 0.1 选择一个已有CRUD项目作为改造对象
  - 推荐选择：你最熟悉、表结构不复杂、有真实业务实体的项目。
  - 不推荐选择：纯练手、没有业务含义、只有单表增删改查的项目。
- [ ] 0.2 定义Agent要解决的业务问题
  - 示例：通过自然语言查询业务数据、生成统计摘要、执行受控修改操作。
  - 至少包含3类操作：只读查询、聚合分析、敏感写操作。
- [ ] 0.3 定义贯穿全程的最小业务闭环
  - 示例：用户提问 -> Agent判断是否需要工具 -> 调用本地业务工具/MCP工具 -> 返回流式答案 -> 前端展示执行链路 -> 敏感操作需要确认。

**问大模型**：

`我有一个Spring Boot + React项目，业务是[你的业务]。请帮我设计一个适合作为Agent学习主线的最小闭环，要求包含只读查询、聚合分析、敏感写操作，并指出哪些需求现在不该做。`

**产物**：

- 一段清晰的业务场景说明
- 3个Agent可执行任务样例
- 1个明确不做的范围列表

**退出标准**：

- 能用一句话说清楚这个Agent解决什么问题
- 能说清哪些操作只是查询，哪些操作必须二次确认
- 不再临时切换主线项目

---

## 阶段1：Spring Boot工程化补强

> 目的：Agent最终还是运行在后端服务里。后端基础不稳，后面工具调用、鉴权、事务、审计都会变得混乱。

- [ ] 1.1 审查现有项目分层
  - Controller只负责参数接收、校验、响应封装
  - Service负责业务规则和事务边界
  - Repository/Mapper只负责数据访问
- [ ] 1.2 补全统一异常处理
  - 全局异常处理器
  - 业务异常类型
  - 参数校验异常
  - 未授权/无权限异常
- [ ] 1.3 梳理事务边界
  - 哪些Service方法需要事务
  - 哪些方法不能放进长事务
  - 理解`@Transactional`自调用失效
- [ ] 1.4 补最小测试
  - 至少覆盖1个核心Service正常场景
  - 至少覆盖1个业务异常场景
  - 至少覆盖1个Controller参数校验场景

**问大模型**：

`请review这个Spring Boot项目的分层、异常处理和事务边界。重点指出Controller是否包含业务逻辑、Service事务是否合理、哪些异常应该被统一处理。`

**产物**：

- 一份项目工程化问题清单
- 修复后的异常处理机制
- 至少3个有价值的测试

**退出标准**：

- 能解释每一层为什么这样分
- 能解释一个业务异常如何从Service传到前端
- 能解释一个事务方法失败后数据库会发生什么

---

## 阶段2：LLM API底层调用

> 目的：先理解HTTP请求、SSE、Token、错误处理，再决定是否使用SDK或框架。

- [ ] 2.1 用Java原生`HttpClient`调用一个LLM API
  - 不使用SDK
  - 先完成非流式调用
  - 明确请求头、请求体、响应体结构
- [ ] 2.2 实现流式SSE解析
  - 能逐段接收模型输出
  - 能处理结束事件
  - 能处理异常事件
- [ ] 2.3 增加基础错误处理
  - 超时
  - 401/403鉴权错误
  - 429限流
  - 5xx服务端错误
- [ ] 2.4 理解Token和上下文窗口
  - 输入Token
  - 输出Token
  - 上下文截断
  - 成本估算
- [ ] 2.5 输出结构化JSON
  - 让模型返回固定JSON结构
  - 处理JSON不合法的情况

**问大模型**：

`请用Java 21的java.net.http.HttpClient写一个最小LLM客户端，支持非流式和SSE流式响应。不要使用SDK，重点解释请求结构、SSE解析、错误处理和Token成本估算。`

**产物**：

- `LlmClient`
- 非流式调用demo
- 流式调用demo
- 错误处理策略
- Token使用记录结构

**退出标准**：

- 能独立解释普通HTTP响应和SSE响应的区别
- 能解释模型API失败时应该给用户什么反馈
- 能保存每次调用的输入Token、输出Token和估算成本

---

## 阶段3：Tool Use / Function Calling

> 目的：让模型从“只会回答”变成“能决定调用本地能力”。

- [ ] 3.1 理解Tool Schema
  - 工具名称
  - 工具描述
  - JSON Schema参数
  - 必填字段
- [ ] 3.2 实现本地工具注册
  - 至少2个只读工具
  - 至少1个敏感写工具
  - 工具需要明确入参和出参
- [ ] 3.3 实现模型触发工具调用
  - 解析模型返回的tool call
  - 执行本地方法
  - 把tool result发回模型
- [ ] 3.4 实现Agent Loop
  - 模型可以连续调用多个工具
  - 设置最大循环次数
  - 设置超时
  - 设置失败退出条件
- [ ] 3.5 增加工具安全边界
  - 工具白名单
  - 参数校验
  - 敏感操作必须进入待确认状态
  - 不允许模型直接执行删除/修改类操作

**问大模型**：

`请帮我设计一个Java版最小Agent Loop：模型可以调用本地工具，工具结果再发回模型，直到模型给最终答案。要求包含最大循环次数、工具白名单、参数校验、敏感操作待确认。`

**产物**：

- 工具注册表
- 至少3个业务工具
- Agent Loop
- 敏感操作待确认机制

**退出标准**：

- Agent能完成“查询 -> 分析 -> 回答”的完整链路
- Agent遇到敏感写操作时不会直接执行
- 能解释Agent Loop什么时候继续、什么时候终止

---

## 阶段4：前端接入第一个Agent闭环

> 目的：尽早把前后端打通。前端不是最后补页面，而是参与定义Agent交互方式。

- [ ] 4.1 React接入流式输出
  - 使用SSE或Fetch Stream
  - 支持加载中、输出中、完成、失败状态
- [ ] 4.2 展示Agent执行链路
  - 用户问题
  - 模型思考摘要
  - 工具调用
  - 工具结果
  - 最终答案
- [ ] 4.3 实现敏感操作确认
  - 后端返回待确认操作
  - 前端展示确认弹窗
  - 用户确认后后端再执行
- [ ] 4.4 增加中断和重试
  - 用户可以停止当前Agent执行
  - 失败后可以基于同一问题重试

**问大模型**：

`请帮我设计React端Agent交互组件，要求支持SSE流式输出、工具调用Timeline、敏感操作确认、中断和重试。不要做营销页，只做实际工作台界面。`

**产物**：

- Agent聊天/任务界面
- 执行链路Timeline
- 敏感操作确认弹窗
- 中断和重试按钮

**退出标准**：

- 用户能在前端完成一次完整Agent任务
- 用户能看懂Agent调用了哪些工具
- 敏感操作必须由用户确认后才执行

---

## 阶段5：MCP协议基础

> 目的：理解MCP为什么存在，以及它和普通Tool Use的边界。MCP不是为了替代所有本地工具，而是为了标准化外部上下文和工具接入。

- [ ] 5.1 理解MCP三大原语
  - Tools：模型可调用的动作
  - Resources：可读取的上下文资源
  - Prompts：可复用的提示模板
- [ ] 5.2 搭建最小MCP Server（stdio）
  - 暴露一个只读业务工具
  - 暴露一个业务Resource
  - 用本地MCP客户端调通
- [ ] 5.3 接入Claude Desktop或其他MCP客户端
  - 验证工具能被发现
  - 验证工具能被调用
  - 观察协议消息
- [ ] 5.4 理解stdio和Streamable HTTP
  - stdio适合本地进程、桌面客户端、开发调试
  - Streamable HTTP适合远程服务、多客户端、服务化部署
- [ ] 5.5 改造成Streamable HTTP
  - 支持多个客户端连接
  - 理解session隔离
  - 处理连接断开和重连

**问大模型**：

`请解释MCP的Tools、Resources、Prompts分别解决什么问题，并用Java MCP SDK写一个最小MCP Server，先支持stdio，再改造成Streamable HTTP。重点解释两种传输方式的适用场景，而不是简单说哪个更高级。`

**产物**：

- stdio MCP Server
- Streamable HTTP MCP Server
- 至少1个Tool
- 至少1个Resource
- session隔离说明

**退出标准**：

- 能解释MCP Tool和普通本地Tool的区别
- 能解释Resource为什么不应该被滥用成Tool
- 能让至少一个MCP客户端成功调用你的Server

---

## 阶段6：框架选型与Agent状态管理

> 目的：从手写Agent Loop过渡到框架，但不能被框架牵着走。先知道自己要什么，再选工具。

- [ ] 6.1 对比候选框架
  - Spring AI
  - LangGraph4j
  - Semantic Kernel Java
- [ ] 6.2 明确选型标准
  - 是否贴合Spring Boot
  - 是否支持工具调用
  - 是否支持流式输出
  - 是否方便接入MCP
  - 是否方便管理状态
  - 社区和文档成熟度
- [ ] 6.3 用选定框架重写最小Agent
  - 保留阶段3的业务工具
  - 保留阶段4的前端交互
  - 不增加新业务功能
- [ ] 6.4 设计对话历史
  - 历史存储位置
  - history key设计
  - TTL策略
  - 上下文压缩策略
- [ ] 6.5 实现Checkpoint
  - 以数据库为主
  - 每完成一个Agent步骤保存一次状态
  - 服务重启后从最近完成步骤继续

**问大模型**：

`请对比Spring AI、LangGraph4j、Semantic Kernel Java在Spring Boot项目中的适配度，重点比较工具调用、流式输出、MCP接入、状态管理和Checkpoint。然后基于我的项目给出一个选型建议。`

**产物**：

- 框架选型记录
- 框架版Agent实现
- 对话历史存储
- Checkpoint表结构
- 重启恢复demo

**退出标准**：

- 能解释为什么选这个框架
- 能解释对话历史和Checkpoint不是一回事
- Agent执行到某个步骤后服务重启，能从最近完成步骤继续

---

## 阶段7：鉴权与权限边界

> 目的：Agent能调用工具以后，权限问题必须明确。模型不能绕过用户权限。

- [ ] 7.1 梳理现有系统权限模型
  - 用户身份
  - 角色
  - 权限点
  - 数据范围
- [ ] 7.2 给Agent请求接入用户身份
  - 每次Agent执行都绑定userId
  - 每次工具调用都带上权限上下文
- [ ] 7.3 工具层做权限校验
  - 查询类工具校验数据范围
  - 写操作校验业务权限
  - 敏感操作二次确认不能替代权限校验
- [ ] 7.4 MCP Server鉴权
  - 理解MCP OAuth 2.1模型
  - Spring Security配置Resource Server
  - 验证无token、有token、无权限三种情况

**问大模型**：

`请帮我设计Agent工具调用的权限模型。要求每次Agent执行绑定用户身份，每次工具调用都校验权限，敏感操作需要二次确认，但二次确认不能替代后端权限校验。`

**产物**：

- Agent权限上下文
- 工具权限校验
- MCP鉴权demo
- 未授权/无权限测试用例

**退出标准**：

- 模型不能调用用户无权使用的工具
- 模型不能查询用户无权访问的数据
- 无token请求MCP Server会被拒绝

---

## 阶段8：可观测性与审计

> 目的：Agent系统出问题时，必须知道它在哪一步失败、调用了什么工具、花了多少钱。

- [ ] 8.1 设计Agent执行事件
  - execution_started
  - model_requested
  - tool_called
  - tool_succeeded
  - tool_failed
  - confirmation_required
  - execution_completed
  - execution_failed
- [ ] 8.2 设计Trace层级
  - 一次Agent执行是一个trace
  - 每次模型调用是一个span
  - 每次工具调用是一个span
  - 每次MCP调用是一个span
- [ ] 8.3 接入OpenTelemetry
  - 先本地输出或接入轻量可视化工具
  - 不一开始追求完整监控平台
- [ ] 8.4 保存审计日志
  - 谁发起
  - 何时发起
  - 调用了什么工具
  - 工具入参摘要
  - 是否执行敏感操作
  - 是否成功

**问大模型**：

`请帮我设计Agent执行链路的OpenTelemetry span层级和审计日志结构。要求能看清一次Agent执行中每次模型调用、工具调用、MCP调用、用户确认和失败原因。`

**产物**：

- Agent事件模型
- Trace/span设计
- 审计日志表
- 一次完整执行的可观察记录

**退出标准**：

- 能定位一次Agent失败发生在哪一步
- 能查到某个用户执行过哪些敏感操作
- 能把一次Agent执行的模型调用和工具调用串起来看

---

## 阶段9：限流、成本统计与多租户隔离

> 目的：把Agent从“能跑demo”推进到“有基本运营边界”。

- [ ] 9.1 设计限流策略
  - 按用户限流
  - 按租户限流
  - 按工具限流
  - 模型调用和普通接口分开限流
- [ ] 9.2 实现Redis令牌桶
  - key包含tenantId/userId
  - 支持窗口内剩余额度查询
  - 超限时返回明确错误
- [ ] 9.3 统计Token和成本
  - 每次模型调用记录Token
  - 每次Agent执行汇总成本
  - 按用户、租户、时间范围查询
- [ ] 9.4 验证多租户隔离
  - 数据查询隔离
  - 历史记录隔离
  - Checkpoint隔离
  - 成本统计隔离
  - Redis key隔离

**问大模型**：

`请帮我设计Agent系统的限流、Token成本统计和多租户隔离方案。要求Redis key、数据库表、查询条件都包含tenantId/userId，并说明哪些地方最容易漏掉隔离条件。`

**产物**：

- Redis限流实现
- Token成本统计表
- 用户/租户成本查询接口
- 多租户隔离检查清单

**退出标准**：

- 超出调用额度时Agent不会继续请求模型
- 能查询任意用户在任意时间段的调用次数和成本
- 不同租户看不到彼此的对话历史、Checkpoint和业务数据

---

## 阶段10：整合成一个可演示系统

> 目的：把前面的能力收束成一个完整项目，而不是一堆分散实验。

- [ ] 10.1 整理完整业务流程
  - 登录
  - 发起Agent任务
  - 流式输出
  - 工具调用展示
  - 敏感操作确认
  - MCP工具调用
  - 审计和成本记录
- [ ] 10.2 做一次端到端演示
  - 普通查询任务
  - 多工具分析任务
  - 敏感写操作任务
  - 权限不足任务
  - 限流触发任务
- [ ] 10.3 写技术复盘
  - 架构图
  - Agent Loop设计
  - MCP接入方式
  - 权限和确认机制
  - Checkpoint设计
  - 可观测性设计
  - 踩坑记录
- [ ] 10.4 判断是否学习A2A
  - 默认不学
  - 只有出现多个Agent跨系统协作、任务委派、角色分工时再学

**问大模型**：

`请帮我review这个Agent项目的最终架构，重点看Agent Loop、MCP接入、权限边界、Checkpoint、可观测性、限流和成本统计是否形成闭环。请指出最可能在真实项目中出问题的地方。`

**产物**：

- 可演示系统
- 端到端演示脚本
- 技术复盘文档
- 后续学习路线

**退出标准**：

- 能现场演示一个完整Agent任务
- 能解释每个核心模块为什么存在
- 能说清当前系统离生产可用还差什么

---

## 事件推进总览

| 阶段 | 推进事件 | 必须产物 |
|---|---|---|
| 阶段0 | 主线项目确定 | 业务闭环、任务样例、不做范围 |
| 阶段1 | 后端工程化过关 | 异常处理、事务边界、最小测试 |
| 阶段2 | 模型调用跑通 | 非流式、SSE、错误处理、Token记录 |
| 阶段3 | 本地Agent Loop跑通 | 工具注册、工具调用、敏感操作待确认 |
| 阶段4 | 前后端Agent闭环跑通 | 流式界面、执行链路、确认弹窗 |
| 阶段5 | MCP Server跑通 | stdio、Streamable HTTP、Tool、Resource |
| 阶段6 | 框架和状态管理落地 | 框架版Agent、历史、Checkpoint |
| 阶段7 | 权限边界跑通 | 用户上下文、工具权限、MCP鉴权 |
| 阶段8 | 可观测性跑通 | trace、span、审计日志 |
| 阶段9 | 运营边界跑通 | 限流、成本统计、多租户隔离 |
| 阶段10 | 系统整合完成 | 演示系统、演示脚本、复盘文档 |

---

## 每阶段固定复盘问题

每完成一个阶段，都回答下面这些问题：

- 这个阶段新增了什么能力？
- 这个能力依赖哪些前置能力？
- 如果真实用户使用，最容易失败在哪里？
- 哪些输入必须校验？
- 哪些操作需要权限？
- 哪些操作需要审计？
- 哪些数据需要按tenantId/userId隔离？
- 这个阶段有没有引入不必要的复杂度？

---

## 不建议现在投入的内容

- 不建议一开始学多Agent协作/A2A，除非已经有明确的多Agent任务拆分场景。
- 不建议一开始上Kafka，只为学习Checkpoint可以先用数据库。
- 不建议一开始追求完整监控平台，先把trace/span和审计日志设计清楚。
- 不建议一开始封装通用Agent平台，先把一个业务Agent做完整。
- 不建议依赖模型直接执行写操作，所有敏感写操作都必须走后端确认和权限校验。

---

## v3相对v2的主要变化

- 去掉周数，改成按事件和产物推进。
- 增加阶段0，先确定贯穿全程的主线项目和业务闭环。
- 把前端集成提前到MCP之前，尽早形成可运行闭环。
- 修正MCP传输表述：stdio适合本地和开发场景，Streamable HTTP适合远程和多客户端服务化场景。
- 框架选型加入Spring AI，与LangGraph4j、Semantic Kernel Java一起比较。
- 明确区分对话历史和Checkpoint。
- 把权限上下文、敏感操作确认、审计、多租户隔离贯穿到后续阶段。
- 把可观测性从“接入监控平台”调整为“先设计trace/span和审计事件，再选择展示方式”。
