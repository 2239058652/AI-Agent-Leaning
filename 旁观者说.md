# 旁观者说

> 一份对 Agent-Learning 项目目标与计划的旁观意见。不是执行指令，是外部视角的提醒。
> 记录时间：2026-07-16，对应学习计划 v3，项目进度已到第5阶段。

---

## 先说结论

计划本身的设计质量高——事件推进、退出标准可验证、克制清单清晰。但**执行已经偏离了阶段0的设定**，用玩具工具替代了真实业务载体，导致阶段3-5 的退出标准形式上过了、实质上没过。越往后走，业务载体缺失的债越大。

---

## 真正做得好的地方

### 1. 事件推进而非周数推进
学习项目按时间排必然脱节，按产物+退出标准推进才现实。这是整份计划最对的设计。

### 2. 退出标准可验证
每阶段都能用"能不能解释/能不能演示"判断，不含糊。比如"能解释 Agent Loop 什么时候继续、什么时候终止"——这种标准没法糊弄。

### 3. 前端提前到阶段4
避免了"后端写一堆最后才接前端"的常见坑。前端参与定义交互方式，而不是最后补页面。

### 4. 区分对话历史和 Checkpoint
这两个概念很多人混在一起，计划明确区分了。对话历史是给模型看的上下文，Checkpoint 是执行断点恢复——把这两件事分开，说明设计者真理解了。

### 5. "不建议现在投入"清单
体现了克制。知道什么不做比知道做什么重要。明确写"A2A 默认不学""不一开始上 Kafka""不追求完整监控平台"，这些判断都对。

---

## 真实问题（按严重程度排）

### 问题1：阶段0实际没做，导致后面失去业务载体（最严重）

v3 计划阶段0 要求：
- 选择一个已有 CRUD 项目作为改造对象
- 定义 Agent 要解决的业务问题
- 至少包含3类操作：只读查询、聚合分析、敏感写操作

但实际执行用的是：
- 查天气
- 查日期
- 计算器
- 查IP

**这是纯玩具，没有业务实体。**

连锁后果：
- 阶段3.5 要求"敏感操作待确认"，但因为没有真实业务，根本没有写操作 → 退出标准没真正满足
- 阶段4.3 敏感操作确认弹窗因此被"跳过" → 这不是跳过，是没做
- 阶段7 权限校验、阶段8 审计、阶段9 多租户隔离 → 全部失去业务载体，只能拿玩具工具演

**这是计划与执行的偏离，不是计划本身的问题，但它让阶段3-5 的退出标准形同虚设。**

建议：补一个最小业务实体（哪怕一张 orders 表），让"敏感写"真实存在。比如"查询订单"是只读，"统计今日 GMV"是聚合，"取消订单"是敏感写。有了这三个，后续权限/审计/隔离才有落地点。

### 问题2：数据库层缺失，但阶段6-9全依赖它

CLAUDE.md 自己标了"无数据库层，暂不需要 @Transactional"。可是：
- 阶段6 Checkpoint 要用数据库
- 阶段8 审计日志要存表
- 阶段9 多租户隔离要数据层

**计划里没有一个阶段明确说"先建数据库/JPA"。** 阶段6 才引入会很被动，因为 Checkpoint 和对话历史一起上，同时还要学框架选型。

建议：在阶段5和6之间补一个"数据层落地"的铺垫，或者把数据库引入明确写进阶段6的前置任务。否则阶段6会变成"同时学框架+学JPA+设计Checkpoint表"，负担过重。

### 问题3：阶段9"多租户隔离"对学习项目过重，且与计划自身矛盾

计划末尾自己说"不建议一开始封装通用 Agent 平台"，但多租户隔离本身就是平台化能力。单人说学习项目，多租户的投入产出比低，容易拖慢对 Agent 核心能力的深入。

建议：把多租户从"必须实现"降级为"设计演练"——画出隔离方案、指出哪些 key/表要带 tenantId，但不一定要全部实现。把省下的精力投到 Checkpoint 恢复和可观测性上，这两个对理解 Agent 更值。

### 问题4：成本统计放阶段9太晚

阶段2.4 就要求"理解 Token 和成本估算"，但实际记录结构要到阶段9 才做。中间阶段6-8 的 Agent 执行都没有成本追踪，阶段8 的可观测性因此不完整（看不到钱）。

建议：阶段2 就落地一个最小 Token 记录表（甚至先写文件都行），后面阶段累加，到阶段9 只做聚合查询，而不是从零搭。

### 问题5：框架选型放阶段6的权衡没说清

先手写再上框架的教学顺序合理，但代价是阶段3-5 的 Agent Loop 在阶段6 会被框架替代重写。计划写"保留业务工具和前端交互"，但 Loop 本身要重写。

建议：在阶段6 明确写"重写 Agent Loop，接受这部分代码废弃"，别让人以为只是"套个框架"。

---

## 小问题

- **文档端口不一致**：CLAUDE.md 说后端 8080，README 说 3180。学习项目也该维护文档一致性，养成习惯。
- **MCP Resources/Prompts 可能被跳过**：阶段5.1 要求理解三大原语，但 mcp-server 只有1个 Tool，没看到 Resource/Prompts 实现。退出标准"解释 Resource 为什么不该被滥用成 Tool"需要实际写一个 Resource 才说得清。

---

## 给执行者的优先级建议

如果只能做一件事，**回头补一个最小业务实体**，而不是继续往后推进。

理由：阶段6-9 的核心价值（Checkpoint、权限、审计、隔离）全部需要业务载体。没有业务实体，后面的阶段会变成"用玩具工具演示生产级能力"，学不到真东西。

补业务实体的最小动作：
1. 加一张业务表（orders / tickets / tasks 任选）
2. 把"查天气"这类工具替换或补充成业务工具
3. 加一个真实的写操作（取消订单/关闭工单）
4. 敏感操作确认弹窗在阶段4.3 补回来

做完这四步，阶段3-5 的退出标准才真正满足，后面阶段才有地基。
